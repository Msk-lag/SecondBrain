[extend]
useDefault = true

[allowlist]
# .env.example はプレースホルダー値のみを含む運用規約のテンプレートファイル
# (実際の秘密情報は .env(git管理外)にのみ置く)。ファイル単位で除外している。
# 将来このファイルに本物の値が誤って入った場合は検知できない点は許容済みのトレードオフ
# (2026-07-08、コードレビュー指摘 A-2 でユーザー承認)。
paths = [
  '''\.env\.example$''',
]
