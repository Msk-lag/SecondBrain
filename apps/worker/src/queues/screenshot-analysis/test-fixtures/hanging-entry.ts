/**
 * テスト専用フィクスチャ: `resize-for-claude.spec.ts` のタイムアウト検証で使う、意図的に
 * 応答しない子プロセスエントリポイント(§ 画像処理のハング・クラッシュ耐性 参照)。
 * 親からの IPC メッセージを受け取っても一切応答せず、プロセスを起動したまま待ち続けることで、
 * `resizeForClaude` が 30 秒のタイムアウトで確実に `SIGKILL` することを検証できるようにする。
 * このファイルは本番のビルド成果物(dist)には含めない(tsconfig.build.json の exclude 参照)。
 */
process.on("message", () => {
  // 意図的に何もしない(タイムアウトを発生させるため)。
});
