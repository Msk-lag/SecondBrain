# 機能設計書: <機能ID> <機能名>

<!-- docs/features/<機能ID>.md として、機能の実装 PR と同一 PR 内で作成・更新する(ADR-0003 規則 DOC-1)。
     記載対象は「契約・判断理由・横断責務」に限定する。内部アルゴリズムやファイル配置の逐次同期は不要。
     生成可能な情報(スキーマ全文等)は正本ファイルへのリンクに留め、重複記載しない。 -->

- 状態: draft | current | superseded
- 対象マイルストーン: <M1-x>
- 最終確認 commit: <SHA>
- 関連 Issue / PR: #<n>
- 関連 ADR: <ADR 番号、なければ「なし」>

## 概要(3行以内)

<この機能が何を解決するか。ユーザーから見た受入成果。>

## スコープ / 非スコープ

- スコープ: <この機能に含むもの>
- 非スコープ: <意図的に含めないもの(将来拡張の含みも書く)>

## 契約(外部から見える約束)

<!-- 機械可読な正本(zod スキーマ・Prisma schema・OpenAPI 等)があるものはパスを示す。文章へ重複記載しない。 -->

- **API**: <エンドポイント一覧+正本スキーマへのパス>
- **DB / データモデル**: <テーブル/エンティティ+正本 schema へのパス。データ所有権(どの package が書き込むか)>
- **ジョブ / イベント**: <キュー名・ペイロード契約・冪等性の扱い>
- **画面**: <ルート・主要状態(通常/ローディング/空/エラー)。デザインハンドオフへのリンク>

## 不変条件・互換性

<壊してはいけない前提。例: 認証必須、既存データとの後方互換、削除時のカスケード方針。>

## データフロー

<入力→処理→保存→出力を5行以内で。非同期処理があればジョブ境界を明示。>

## エラー・セキュリティ

- エラー: <主要な失敗経路と応答(エラー形式は共通規約参照)>
- セキュリティ: <認可の単位、入力検証、秘密情報の扱い、外部送信の有無>

## テスト観点

<受入条件に対応するテストの所在。境界値・失敗経路のテストを明記(diff カバレッジ100%は品質を保証しないため、ここで観点を担保する)。>

## 設計判断と理由

<この機能で行った選択とトレードオフ(ADR に昇格しない粒度のもの)。>

## 既知の制約・未解決事項

<意図的に残した制約、先送りした Issue へのリンク。>
