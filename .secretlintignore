node_modules
dist
build
coverage
report
pnpm-lock.yaml

# 実際の秘密情報を含むローカルファイル(git 管理外。--no-gitignore 運用のため明示的に除外)
.env

# プレースホルダー値のみを含むテンプレートファイル(意図的に公開対象。.gitignore の !.env.example と対応)。
# 将来このファイルに本物の値が誤って入った場合は検知できない点は許容済みのトレードオフ
# (2026-07-08、コードレビュー指摘 A-2 でユーザー承認)。
.env.example
